# Learn more about configuring your app at https://shopify.dev/docs/apps/tools/cli/configuration

client_id = "d3130acbe83c050018a661bac7d41211"
application_url = "https://example.com"
embedded = true
name = "Multi Vendor WhatsApp"

[access_scopes]
# Learn more at https://shopify.dev/docs/apps/tools/cli/configuration#access_scopes
# read_themes: el panel comprueba si el bloque ya está colocado en el tema
scopes = "write_products,read_themes"
optional_scopes = [ ]
use_legacy_install_flow = false

[webhooks]
api_version = "2026-07"

  [[webhooks.subscriptions]]
  uri = "/webhooks/app/uninstalled"
  topics = [ "app/uninstalled" ]

  [[webhooks.subscriptions]]
  uri = "/webhooks/app/scopes_update"
  topics = [ "app/scopes_update" ]

  # Obligatorios para publicar en la App Store (privacidad GDPR/CCPA).
  # Se atienden en app/routes/webhooks.compliance.jsx
  [[webhooks.subscriptions]]
  uri = "/webhooks/compliance"
  compliance_topics = [ "customers/data_request", "customers/redact", "shop/redact" ]

[app_proxy]
# La tienda llama a /apps/whatsapp-router/... y Shopify lo reenvía aquí.
# El CLI actualiza esta URL automáticamente durante `shopify app dev`.
url = "https://example.com/proxy"
subpath = "whatsapp-router"
prefix = "apps"

[auth]
redirect_urls = [ ]

[build]
automatically_update_urls_on_dev = true
