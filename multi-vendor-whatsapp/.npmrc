engine-strict=true
; shamefully-hoist es una opción exclusiva de pnpm y este proyecto usa npm:
; npm la rechazaba imprimiendo un aviso en cada comando. Si algún día se
; cambia a pnpm, vuelve a añadir la línea "shamefully-hoist=true".
